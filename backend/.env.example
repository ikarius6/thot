# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database Configuration
# For SQLite: sqlite:///./thot.db
DATABASE_URL=sqlite:///./thot.db

# LLM Model Configuration
# Overrides database setting if provided here (optional implementation, currently logic is in DB)
# But good to document for future use or reference
LLM_MODEL=huihui_ai/qwen3-vl-abliterated:8b
